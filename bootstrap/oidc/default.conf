server {
    listen 8080;
    root /usr/share/nginx/html;
    default_type application/json;
    location = /.well-known/openid-configuration { try_files $uri =404; }
    location = /openid/v1/jwks { try_files $uri =404; }
    location / { return 404; }
}
