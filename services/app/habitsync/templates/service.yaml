apiVersion: v1
kind: Service
metadata:
  name: habitsync
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "habitsync.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "habitsync.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
